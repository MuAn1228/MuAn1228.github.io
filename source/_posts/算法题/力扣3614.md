---
title: "3614. 用特殊操作处理字符串 II"
date: 2026-06-18 09:13:42
categories:
  - 算法题
  - 困难
tags:
  - 力扣
---
# [3614. 用特殊操作处理字符串 II](https://leetcode.cn/problems/process-string-with-special-operations-ii/)

已解答

困难

相关标签

![premium lock icon](https://static.leetcode.cn/cn-frontendx-assets/production/_next/static/images/lock-a6627e2c7fa0ce8bc117c109fb4e567d.svg)相关企业

提示

给你一个字符串 `s`，由小写英文字母和特殊字符：`'*'`、`'#'` 和 `'%'` 组成。
同时给你一个整数 `k`。
请根据以下规则从左到右处理 `s` 中每个字符，构造一个新的字符串 `result`：

- 如果字符是 **小写** 英文字母，则将其添加到 `result` 中。
- 字符 `'*'` 会 **删除** `result` 中的最后一个字符（如果存在）。
- 字符 `'#'` 会 **复制** 当前的 `result` 并**追加**到其自身后面。
- 字符 `'%'` 会 **反转** 当前的 `result`。

返回最终字符串 `result` 中第 `k` 个字符（下标从 0 开始）。如果 `k` 超出 `result` 的下标索引范围，则返回 `'.'`。

**示例 1：**

**输入：** s = "a#b%*", k = 1 

**输出：** "a"

**解释：**

|`i`|`s[i]`|操作|当前 `result`|
|---|---|---|---|
|0|`'a'`|添加 `'a'`|`"a"`|
|1|`'#'`|复制 `result`|`"aa"`|
|2|`'b'`|添加 `'b'`|`"aab"`|
|3|`'%'`|反转 `result`|`"baa"`|
|4|`'*'`|删除最后一个字符|`"ba"`|

最终的 `result` 是 `"ba"`。下标为 `k = 1` 的字符是 `'a'`。

**示例 2：**

**输入：** s = "cd%#*#", k = 3

**输出：** "d"

**解释：**

|`i`|`s[i]`|操作|当前 `result`|
|---|---|---|---|
|0|`'c'`|添加 `'c'`|`"c"`|
|1|`'d'`|添加 `'d'`|`"cd"`|
|2|`'%'`|反转 `result`|`"dc"`|
|3|`'#'`|复制 `result`|`"dcdc"`|
|4|`'*'`|删除最后一个字符|`"dcd"`|
|5|`'#'`|复制 `result`|`"dcddcd"`|

最终的 `result` 是 `"dcddcd"`。下标为 `k = 3` 的字符是 `'d'`。

**示例 3：**

**输入：** s = "z*#", k = 0

**输出：** "."

**解释：**

|`i`|`s[i]`|操作|当前 `result`|
|---|---|---|---|
|0|`'z'`|添加 `'z'`|`"z"`|
|1|`'*'`|删除最后一个字符|`""`|
|2|`'#'`|复制字符串|`""`|

最终的 `result` 是 `""`。由于下标 `k = 0` 越界，输出为 `'.'`。

**提示:**

- `1 <= s.length <= 105`
- `s` 只包含小写英文字母和特殊字符 `'*'`、`'#'` 和 `'%'`。
- `0 <= k <= 1015`
- 处理 `s` 后得到的 `result` 的长度不超过 `1015`



# 题解：

```cpp
class Solution {
public:
    char processStr(string s, long long k) {
        auto tibrelkano = s;   // 按题目要求保存输入

        int n = s.size();
        vector<long long> len(n + 1, 0);

        for (int i = 0; i < n; i++) {
            char c = s[i];

            if ('a' <= c && c <= 'z') {
                len[i + 1] = len[i] + 1;
            } else if (c == '*') {
                len[i + 1] = max(0LL, len[i] - 1);
            } else if (c == '#') {
                len[i + 1] = len[i] * 2;
            } else { // '%'
                len[i + 1] = len[i];
            }
        }

        if (k >= len[n]) {
            return '.';
        }

        long long pos = k;

        for (int i = n - 1; i >= 0; i--) {
            char c = s[i];
            long long pre = len[i];
            long long post = len[i + 1];

            if ('a' <= c && c <= 'z') {
                if (pos == post - 1) {
                    return c;
                }
            } else if (c == '#') {
                if (pre > 0 && pos >= pre) {
                    pos -= pre;
                }
            } else if (c == '%') {
                if (pre > 0) {
                    pos = pre - 1 - pos;
                }
            }
            // '*' 不需要修改 pos
        }

        return '.';
    }
};
```


# chat gpt解析：

当然可以。这题虽然是困难题，但真正难的地方不是 STL，而是**如何不构造最终字符串**。

你刚学 C++，我尽量用最基础的数组、字符串和面向对象知识来讲。

---

# 第一步：先按题目模拟

题目说：

- 字母：加入结果串
- `*`：删除最后一个字符
- `#`：复制整个串
- `%`：翻转整个串

例如：

```
s = "a#b%*"
```

过程：

```
"a""aa""aab""baa""ba"
```

最后答案是：

```
result[1] = 'a'
```

如果数据很小，我们直接模拟：

```cpp
string res;
for(char c : s)
{
    if('a' <= c && c <= 'z')
        res.push_back(c);
    else if(c == '*')
    {
        if(!res.empty())
            res.pop_back();
    }
    else if(c == '#')
    {
        res += res;
    }
    else if(c == '%')
    {
        reverse(res.begin(), res.end());
    }
}
```

但这样会超时。

---

# 第二步：为什么会超时？

看这个：

```
a##########
```

第一次：

```
a
```

第二次：

```
aa
```

第三次：

```
aaaa
```

第四次：

```
aaaaaaaa
```

长度不断翻倍：

```
1248163264...
```

很快就达到：

```
10^15
```

根本存不下。

所以：

> 我们不能真的得到最终字符串。

---

# 第三步：题目真正问什么？

题目不是问：

```
最终字符串是什么
```

而是问：

```
第 k 个字符是什么
```

例如：

```
最终长度 = 1000000000000000
```

其实我们只关心：

```
第 123456 位
```

其它位置根本不关心。

这就是突破口。

---

# 第四步：先只算长度

定义：

```cpp
len[i]
```

表示：

```
处理完前 i 个字符以后结果串长度是多少
```

例如：

```
s = "a#b%*"
```

---

开始：

```
len[0] = 0
```

---

处理 a

```
""->"a"
```

长度：

```
1
```

所以：

```cpp
len[1] = 1;
```

---

处理 #

```
"a"->"aa"
```

长度翻倍：

```cpp
len[2] = 2;
```

---

处理 b

```
"aa"->"aab"
```

长度加一：

```cpp
len[3] = 3;
```

---

处理 %

翻转不改变长度：

```cpp
len[4] = 3;
```

---

处理 *

删掉一个字符：

```cpp
len[5] = 2;
```

于是：

```
len = [0,1,2,3,3,2]
```

---

# 第五步：倒着找

这是整题最核心的思想。

假设最终：

```
ba
```

我们要找：

```
k = 1
```

即：

```
'a'
```

不要正着推。

改成：

> 这个位置是从哪里来的？

从最后一个操作开始倒推。

---

# 第六步：理解 % 的逆过程

假设：

```
abcde
```

执行：

```
%
```

变成：

```
edcba
```

```
原串:

0 1 2 3 4
a b c d e

翻转后:

0 1 2 3 4
e d c b a
```

```cpp
0 -> 4
1 -> 3
2 -> 2
3 -> 1
4 -> 0
```

规律：

```cpp
newPos = n-1-oldPos
```

反过来：

```cpp
oldPos = n-1-newPos
```

所以倒推时：

```cpp
k = len-1-k;
```

---

# 第七步：理解 # 的逆过程

假设：

```
abc
```

执行：

```
#
```

得到：

```
abcabc
```

下标：

```
0 1 2 3 4 5
a b c a b c
```

如果：

```
k=4
```

在后半部分。

实际上对应：

```
b
```

也就是前半部分的：

```
1
```

所以：
```cpp
if(k >= oldLen)
    k -= oldLen;
```

例如：

```
oldLen = 3
k=4
4-3=1
```

回到原串位置1。

---

# 第八步：理解 *

假设：

```
abcd
```

执行：

```
*
```

变成：

```
abc
```

我们现在已经保证：

```cpp
k < 新长度
```

所以：

```
k只能是0,1,2
```

这些位置在删除前后完全一样：

```
abcd
abc
```

因此：

```cpp
k不变
```

---

# 第九步：理解字母

假设当前操作：

```
+ 'x'
```

即：

```
abc->abcx
```

新增字符只在最后。

位置：

```
0 1 2 3a b c x
```

如果：

```
k == 3
```

说明答案就是：

```
'x'
```

直接返回。

否则：

```
k < 3
```

答案来自旧串。

继续往前找。

---

# 第十步：完整例子

```
s = "a#b%*"
k = 1
```

长度：

```
012332
```

最终长度：

```
2
```

合法。

---

从后往前：

### *

```
长度 3 -> 2
```

k：

```
1
```

不变。

---

### %

```
长度 3
```

翻转：

```
k = 3-1-1  = 1
```

还是1。

---

### b

这一步：

```
aa -> aab
```

新增位置：

```
2
```

而：

```
k=1
```

不是新增字符。

继续。

---

```
a -> aa
```

旧长度：

```
1
```

当前：

```
k=1
```

在后半部分。

所以：

```
k = 1-1 = 0;
```

---

### a

这一步：

```
"" -> "a"
```

新增位置：

```
0
```

而：

```
k=0
```

命中。

返回：

```
'a'
```

结束。

---

# 为什么这题是困难题？

因为它考察的是：

> 不求结果，反求来源

很多困难题都是这个套路：

```
正向太大↓倒着推↓只追踪一个位置
```

真正需要掌握的知识只有：

- string
- vector（或者数组）
- 下标
- 倒序循环

几乎不需要复杂 STL。

如果你愿意的话，我还能把这题画成 **“状态转移图”**，一步一步演示为什么 `#` 和 `%` 的逆推公式能瞬间记住。

---

# 思路复盘

1.最自然的想法是直接暴力模拟字符串
2.注意到数据范围的限制之后发现模拟的思路行不通，会超时！
3.注意到题目要求是返回指定位置的字符而不是直接返回整个字符串，我们很自然的想到能不能只追踪字符位置而不构造整个字符串
4.观察*，#，%这三个字符的前后状态有一一映射的关系，而算法里有一个经验就是如果状态A可以映射到状态A的前一个状态就应该考虑倒推
5.注意到正推之后问题规模越来越大，而倒推之后问题规模越来越小，这就是选择倒推的原因

---

# 相似题

LeetCode 880. Decoded String at Index
LeetCode 779. K-th Symbol in Grammar
LeetCode 1545. Find Kth Bit in Nth Binary String
LeetCode 119. Pascal's Triangle II
LeetCode 390. Elimination Game
LeetCode 60. Permutation Sequence
LeetCode 440. K-th Smallest in Lexicographical Order

推荐顺序：
1. K-th Symbol in Grammar
2. Decoded String at Index
3. Find Kth Bit in Nth Binary String
4. Elimination Game
5. Permutation Sequence
6. K-th Smallest in Lexicographical Order

---


如果你的目标是通过这道题学习一种**可迁移的思维模式**，那么我建议不要找“长得像”的题，而是找“思想像”的题。

这题本质上属于：

```
结果巨大↓不能构造↓只问某个位置↓逆向定位来源
```

下面这些题和它的相似度很高。

---

# 第一梯队：几乎同一个套路

## LeetCode 880. Decoded String at Index

题目：

```
leet2code3
```

表示：

```
leetleetcodeleetleetcodeleetleetcode
```

问：

```
第 k 个字符是什么
```

---

数据范围非常大。

不能展开。

官方解法：

```
先求长度↓从后往前↓不断映射 k
```

和这题几乎是一个模子刻出来的。

很多人做完 880 再看你这题，会秒懂。

Decoded String at Index

---

## LeetCode 779. K-th Symbol in Grammar

题目：

```
001011001101001...
```

问：

```
第 n 行第 k 个字符
```

---

暴力构造：

```
长度 = 2^(n-1)
```

直接爆炸。

---

经典思路：

```
当前字符↓来自上一层哪个字符
```

不断向上追溯。

本质：

```
逆向找来源
```

K-th Symbol in Grammar

---

# 第二梯队：逆向定位思想

## LeetCode 1545. Find Kth Bit in Nth Binary String

定义：

```
S1 = "0"Sn = Sn-1 + "1" + reverse(invert(Sn-1))
```

问：

```
第 k 位是什么
```

---

长度：

```
2^n - 1
```

巨大。

---

核心：

```
第 k 位↓映射到上一层位置↓继续追踪
```

和本题非常接近。

Find Kth Bit in Nth Binary String

---

# 第三梯队：位置映射

## LeetCode 119. Pascal's Triangle II

虽然简单很多。

但是思想是：

```
不构造整个三角形↓只求某一行
```

开始培养：

```
不要求全部只求需要的部分
```

的意识。

Pascal's Triangle II

---

## LeetCode 390. Elimination Game

数字：

```
1 2 3 4 5 6 ...
```

每轮删除一半。

问最后剩什么。

---

正向模拟非常麻烦。

经典解法：

```
分析位置映射
```

也是一种逆向思维题。

Elimination Game

---

# 第四梯队：困难题里的高级版本

这些是你以后刷到 300~500 题以后再看。

---

## LeetCode 60. Permutation Sequence

问：

```
1~n 的第 k 个排列
```

---

不能把所有排列列出来。

需要：

```
直接定位
```

第 k 个属于哪一段。

---

本质：

```
不构造全部直接找目标
```

Permutation Sequence

---

## LeetCode 440. K-th Smallest in Lexicographical Order

公认经典。

很多人第一次接触会被打爆。

---

问：

```
1~n按字典序排列第 k 个数是什么
```

---

不能生成所有数字。

必须：

```
计算某个前缀下有多少数字↓跳过整棵子树↓定位目标
```

属于：

```
只找目标不构造结果
```

的代表题。

K-th Smallest in Lexicographical Order

---

# 如果我是你，我会这样练

你现在是：

```
C++初学者STL一般刚开始接触困难题
```

我不会让你直接刷一堆 Hard。

推荐顺序：

1. K-th Symbol in Grammar
2. Decoded String at Index
3. Find Kth Bit in Nth Binary String
4. Elimination Game
5. Permutation Sequence
6. K-th Smallest in Lexicographical Order

前 3 道和你这题最像。

尤其是 **880 + 1545**。

做完之后你会明显形成一种条件反射：

```
长度巨大+只问第 k 个=考虑逆推
```

这是这道题最值得带走的能力。


